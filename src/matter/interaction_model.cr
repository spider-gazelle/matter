# Interaction Model: attribute/command/event paths, status codes and TLV message
# structures shared by the device (server) and controller (client) sides.
require "./interaction_model/paths"
require "./interaction_model/status_code"
require "./interaction_model/tlv_messages"
